# Скопируйте этот файл в .env и подставьте реальные значения.
# .env не коммитится (см. .gitignore) — те же значения для запуска без Docker
# (dotnet run) лежат в dotnet user-secrets у Authorization.API и Gateway.API.

AUTH_JWT_SECRET_KEY=
AUTH_EMAIL_PASSWORD=

services:
  profiles-api:
    build:
      context: .
      dockerfile: Profiles/Profiles.Api/Dockerfile
    container_name: profiles-api
    depends_on:
      sql-server:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    ports:
      - "8001:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=Server=sql-server;Database=MmcProfilesDb;User Id=sa;Password=${SQL_SA_PASSWORD};TrustServerCertificate=True;
      - RabbitMqSettings__Host=rabbitmq
      - RabbitMqSettings__Username=guest
      - RabbitMqSettings__Password=guest
      - GrpcClients__Authorization=http://authorization-api:8080
      - GrpcClients__Offices=http://offices-api:8080
      - GrpcClients__Services=http://services-api:8080
    networks:
      - medical-network
